# Copy to .env before `docker compose up -d`.
# Generate a token with:  python -c "import secrets; print(secrets.token_urlsafe(32))"
CASEBROKER_TOKENS=replace-me-with-a-long-random-token
BROKER_DOMAIN=broker.example.org
